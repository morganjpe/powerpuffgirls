import EpisodeDescription from '../features/episodeDescription';

const EpisodeDescriptionPage = () => {
  return <EpisodeDescription number={0} showId={6771} />;
};

export default EpisodeDescriptionPage;
