const SearchBar = () => {
  return (
    <form>
      <label htmlFor="search">
        search
        <input id="search" name="search" type="text" />
      </label>
    </form>
  );
};

export default SearchBar;
